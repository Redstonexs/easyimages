# Docker 部署

当前仓库内置 Dockerfile 和 `docker-compose.yml`。

## Docker Compose

```bash
git clone https://github.com/Redstonexs/easyimages.git
cd easyimages
docker-compose up -d
docker-compose logs -f
```

默认配置：

```yaml
services:
  easyimage:
    build: .
    container_name: easyimage
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
      - ./i:/app/i
      - ./admin/logs:/app/admin/logs
```

## Docker CLI

```bash
docker build -t easyimage .
docker run -d \
  --name easyimage \
  -p 8080:8080 \
  -v "$PWD/config:/app/config" \
  -v "$PWD/i:/app/i" \
  -v "$PWD/admin/logs:/app/admin/logs" \
  easyimage
```

## 从 PHP 版本迁移到 Docker

```bash
cp /path/to/php/config/config.php config/
cp /path/to/php/config/config.guest.php config/
cp /path/to/php/config/api_key.php config/
docker-compose up -d
docker-compose logs -f
```

详细说明见 [从PHP迁移到Go版本](从PHP迁移到Go版本.md)。

## 历史第三方教程

- Derek DDS Docker 版本：https://github.com/DDS-Derek/EasyImage-Docker
- Docker Hub：https://hub.docker.com/r/ddsderek/easyimage
- 宝塔版教程：https://blog.laoda.de/archives/aapanel-install-easyimage
- Docker Compose 教程：https://blog.laoda.de/archives/docker-compose-install-easyimage

这些教程可能基于旧 PHP 版本或第三方镜像，路径和配置可能与当前 Go 仓库不同。
